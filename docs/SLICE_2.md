# Slice 2 — deterministic integration

## Added

- perception runtime contracts;
- candidate validation and dependency ordering;
- template resolution / dynamic T creation;
- exact entity resolution + ambiguity plans;
- deixis through interaction context;
- C/P semantic domain routing;
- H-only integration path for own response;
- semantic turn event in H;
- FOLLOW chain between experienced turns;
- activation seed requests as boundary to future IgnitionEngine;
- indexes required by integration;
- integration unit tests.

## Not added

No external LLM call and no excitation mutation are present in this slice.
The integration layer returns seed requests; the next slice will own `x/f/g/h/t/ν`.
