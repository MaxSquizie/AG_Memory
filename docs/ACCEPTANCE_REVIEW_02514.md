# Разбор 20260907_004610_+0300

## Измеренный результат

| Показатель | Предыдущий прогон | Новый прогон |
|---|---:|---:|
| Semantic PASS | 25 | 30 |
| Semantic FAIL | 75 | 70 |
| Runtime OK | 89 | 88 |
| Runtime ERROR | 11 | 12 |

Тексты и номера сопоставлены по manifest. Новые PASS: 23, 39, 40, 52, 85. Потерь прежних PASS нет. Кейс 60 перешёл из неверного успешного разбора в явный отказ до commit.

## Почему 106 предыдущих новых тестов не обеспечили высокий acceptance

Тесты v0.25.13 проверяли builder отдельно и recovery отдельно, в том числе передачу scope в canonical integration. Они не выполняли последовательность `build → nominal rewrite → parse → recovery` целиком. Новый живой trace показал, что middle-stage rewrite отменяет результат builder. Этот пропуск покрытия исправлен в v0.25.14.

В turn_002 исходный graph содержит `FRAME` для «а Мария — журнал», но perception выдаёт `predicate=журнал, SUBJECT=Мария`. В turn_022 аналогично возникает `predicate=казань`. В turn_026 корректно размеченная тройная цепочка заканчивается `no canonical roles remain available` из-за ошибочно повторно созданного предиката.

Исправление сохраняет ownership клаузы. Имена файлов/версий и новые тесты перечислены в VERSION_02514.md.

## Проверяемые границы

- Unit/integration coverage использует реальные morphology/parser/template completion/integration/inference.
- Только выходы ограниченных semantic probes задаются фикстурами.
- Положительные и отрицательные проверки nominal boundary предотвращают отключение всех именных конструкций ради эллипсиса.
- Полный условный сценарий проверяет отсутствие доказательства без посылки и появление proof support после фактической посылки.
- В новую offline-статистику не включаются фиктивные результаты повторного прогона LM Studio.

Следующая проверка пользователем — тот же 100-case GUI acceptance. Его delta относительно 30/100 покажет реальный эффект правки; заранее оценка не подставляется.
