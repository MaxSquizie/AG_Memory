# Slice 12.1

- Predicate English naming no longer invalidates already parsed neighbouring frames.
- Added deterministic common Russian→English predicate mappings including `давать → give`.
- Unknown predicate labels are proposed from the local clause, then verified by a separate numeric probe.
- Predicate-symbol prompts contain no examples/few-shot anchors.
- Added `predicate_symbol_verify` to the GUI prompt editor.
