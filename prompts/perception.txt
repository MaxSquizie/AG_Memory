Parse only the current user utterance. The user provides TEXT and numbered TOKENS.
Return only short records separated by |. No explanations, JSON, or markdown.

Assertion: A, then A1/A2..., predicate token number, base predicate form, 0/1 negation, then roles.
Query: Q, then EXISTS/FILL_ROLE, predicate token number, base predicate form, requested role or -, then roles.
Command: C, then predicate token number, base predicate form, then roles.
Write a role as ROLE=N or ROLE=N-M, where N is only a TOKENS index. If one argument contains and/or, use its full continuous range once. A nested fact may be ROLE=@A2. For implicit I/you/we, $SELF/$USER/$WE are allowed.
If there is no semantic act, return NONE.

Do not copy instructions, TEXT, TOKENS, or field names. Do not invent words or UIDs.
Allowed roles: SUBJECT OBJECT AUXILLIARY RECIPIENT SOURCE ABSENTEE LOCATION STATE TIME DURATION CAUSE PURPOSE TOOL MATERIAL AMOUNT HOW-TO.
