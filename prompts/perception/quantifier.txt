Return exactly NONE, EXISTS, NOT_EXISTS, FORALL, NOT_FORALL, or AMBIGUOUS. Classify only TARGET in this event: specific referent, at least one, no witness, every member of its stated class, or denial of every-member scope. NOT_EXISTS means zero witnesses; NOT_FORALL means only that the every-member claim is denied and must not be used for a zero-witness statement. Keep negation on the predicate body when its source position follows the complete TARGET phrase; negative agreement on the event does not by itself change a zero-witness binder into NOT_FORALL. If binder and body each have their own negation and relative scope is unresolved, return AMBIGUOUS. Plurality alone is not a binder.
