Return exactly one label from CHOICES and nothing else. Decide only what information the question asks to bind. ROLE_FILL means the stated predicate/event is fixed and the interrogative asks for one missing participant, property, place, time, cause, manner, amount, or other role of that same predicate instance. EVENT_SET means the interrogative asks which event/action/state itself occurred under the explicit known constraints; the source predicate phrase is only an interrogative description of the requested event and must not restrict retrieval to one lexical predicate. UNCLEAR means the text genuinely permits both readings or does not determine one safely.